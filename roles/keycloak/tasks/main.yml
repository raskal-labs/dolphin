---
- name: Ensure dependencies are installed
  apt:
    name:
      - default-jdk
      - unzip
      - curl
    state: present
    update_cache: yes

- name: Placeholder for Keycloak install
  debug:
    msg: "Keycloak installation steps go here"
