---
# roles/caddy-reverse/defaults/main.yml

# Email used for ACME/Let’s Encrypt registration by Caddy
caddy_email: "labs@raskal.io"

# Root domain (optional landing page / health-check)
# If you decide you don't want a root site, you can just comment out
# the corresponding block in Caddyfile.j2 later.
caddy_root_domain: "raskal.io"

# Upstreams (internal service addresses)

# NetBird LXC
caddy_netbird_upstream: "192.168.0.32"
caddy_netbird_dashboard_port: 80
caddy_netbird_api_port: 33073
caddy_netbird_signal_http_port: 80
caddy_netbird_signal_grpc_port: 10000
caddy_netbird_relay_port: 33080

# Keycloak LXC (file-select)
caddy_keycloak_upstream: "192.168.0.36"

# Proxmox node (gamecube)
caddy_proxmox_upstream: "192.168.0.20"
caddy_proxmox_port: 8006

# Domains
caddy_netbird_domain: "randnet.raskal.io"
caddy_keycloak_domain: "file-select.raskal.io"
caddy_adguard_domain: "link-cable.raskal.io"
caddy_proxmox_domain: "gamecube.raskal.io"
